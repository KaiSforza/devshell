name: "Publish every Git push to attrs to FlakeHub"
on:
  push:
    branches:
      - "attrs"
jobs:
  flakehub-publish:
    runs-on: "ubuntu-latest"
    permissions:
      id-token: "write"
      contents: "read"
    steps:
      - uses: "actions/checkout@v4"
      - uses: "DeterminateSystems/determinate-nix-action@v3"
      - uses: "DeterminateSystems/flakehub-push@main"
        with:
          name: "KaiSforza/devshell"
          rolling: true
          visibility: "public"
          include-output-paths: true
